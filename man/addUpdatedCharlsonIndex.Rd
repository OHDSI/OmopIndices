% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addUpdatedCharlsonIndex.R
\name{addUpdatedCharlsonIndex}
\alias{addUpdatedCharlsonIndex}
\title{Add Updated Charlson Comorbidity Index (CCI) value based on Quan et al. (2011)}
\usage{
addUpdatedCharlsonIndex(
  x,
  indexDate = "cohort_start_date",
  ageAdjusted = TRUE,
  window = c(-Inf, 0),
  conceptSet = NULL,
  nameStyle = "charlson_index",
  categories = NULL,
  name = tableName(x)
)
}
\arguments{
\item{x}{A \code{cdm_table} object, it mus contain \code{person_id} or \code{subject_id} as
columns.}

\item{indexDate}{A character string that points to a \code{Date} column in the \code{x}
table.}

\item{ageAdjusted}{Whether to calculate the Age-Adjusted Comorbidity Index (TRUE) or not (FALSE)}

\item{window}{Window to asses \verb{Charlson index} in, it must be a vector of two numeric values \code{c(min, max)}. Window times refer to days since \code{indexDate}.}

\item{conceptSet}{It can either be a \if{html}{\out{<codelist>}}, <codelist_with_details> or <concept_set_expression> object. It must contain \code{congestive_heart_failure}, \code{dementia}, \code{chronic_pulmonary_disease}, \code{connective_tissue_disease}, \code{mild_liver_disease}, \code{hemiplegia}, \code{severe_chronic_kidney_disease}, \code{diabetes_with_complication}, \code{any_malignancy}, \code{moderate_or_severe_liver_disease}, \code{metastatic_solid_tumor}, \code{aids} as concepts. If \code{NULL} concepts will be retrieved using the OmopConcepts package.}

\item{nameStyle}{A character string with the name of the new column.}

\item{categories}{Named list of categories to group the values. If NULL the
risk score is returned as numeric.}

\item{name}{A character string with the name of the new table. If \code{NULL} a
temporary table will be created.}
}
\value{
The table \code{x} with a new column column with the corresponding
Charlson index value.
}
\description{
Add Updated Charlson Comorbidity Index (CCI) value based on Quan et al. (2011)
}
\examples{
{
library(OmopIndices)
library(omock)

cdm <- mockCdmFromDataset()
cdm <- cdm |>
 mockCohort()

conceptSet <- list(
 "congestive_heart_failure" = 319835L,,
 "dementia" = 4182210L,
 "chronic_pulmonary_disease" = 255573L,
 "connective_tissue_disease" = 4134537L,
 "mild_liver_disease" = 194984L,
 "moderate_or_severe_liver_disease" = 4212540L,
 "diabetes_with_complication" = 42538715L,
 "hemiplegia" = 374022L,
 "severe_chronic_kidney_disease" = 46271022L,
 "any_malignancy" = 4180914L,
 "metastatic_solid_tumor" = 432851L,
 "aids" = 4267414L)

cdm$cohort |>
  addUpdatedCharlsonIndex(conceptSet = conceptSet)
}
}
