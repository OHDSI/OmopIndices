% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adaptedElectronicRiskScore.R
\name{addAdaptedElectronicRiskScore}
\alias{addAdaptedElectronicRiskScore}
\title{Add the adapted electronic risk score as defined in Politi et al:
\if{html}{\out{<xxx>}}}
\usage{
addAdaptedElectronicRiskScore(
  x,
  indexDate = "cohort_start_date",
  window = c(-365, 0),
  conceptSet = NULL,
  categories = list(fit = c(0, 0.12), mild = c(0.12, 0.24), moderate = c(0.24, 0.36),
    severe = c(0.36, 1)),
  nameStyle = "aers",
  name = tableName(x)
)
}
\arguments{
\item{x}{A \code{cdm_table} object, it mus contain \code{person_id} or \code{subject_id} as
columns.}

\item{indexDate}{A character string that points to a \code{Date} column in the \code{x}
table.}

\item{window}{Window to asses \verb{adapted electronic risk score} in, it must be a vector of two numeric values \code{c(min, max)}. Window times refer to days since \code{indexDate}.}

\item{conceptSet}{It can either be a \if{html}{\out{<codelist>}}, <codelist_with_details> or <concept_set_expression> object. It must contain \code{activity_limitation}, \code{anemia}, \code{arthritis}, \code{atrial_fibrillation}, \code{chronic_kidney_disease}, \code{cerebrovascular_disease}, \code{diabetes}, \code{dizziness}, \code{dyspnea}, \code{falls}, \code{foot_problem}, \code{fragility_fracture}, \code{hearing_impairment}, \code{heart_failure}, \code{heart_valve_disorder}, \code{housebound}, \code{hypertension}, \code{hypotension}, \code{ischemic_heart_disease}, \code{memory_cognitive_disorder}, \code{mobility_transfer}, \code{osteoporosis}, \code{parkinsonism_tremor}, \code{peptic_ulcer}, \code{peripheral_vascular_disease}, \code{care_requirement}, \code{respiratory_disease}, \code{skin_ulcer}, \code{sleep_disturbance}, \code{social_vulnerability}, \code{thyroid_disease}, \code{urinary_incontinence}, \code{urinary_system_disease}, \code{visual_impairment}, \code{weight_loss_anorexia} as concepts. If \code{NULL} concepts will be retrieved using the OmopConcepts package.}

\item{categories}{Named list of categories to group the values. If NULL the
risk score is returned as numeric.}

\item{nameStyle}{A character string with the name of the new column.}

\item{name}{A character string with the name of the new table. If \code{NULL} a
temporary table will be created.}
}
\value{
The \code{x} table with a new column added with the adapted electronic
risk score of the patient.
}
\description{
Add the adapted electronic risk score as defined in Politi et al:
\if{html}{\out{<xxx>}}
}
