% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/polypharmacy.R
\name{addPolypharmacyCount}
\alias{addPolypharmacyCount}
\title{Add the maximum number of ingredients an individual is exposed simultaneouly
in a certain window.}
\usage{
addPolypharmacyCount(
  x,
  indexDate = "cohort_start_date",
  window = c(0, 0),
  nameStyle = "polypharmacy_count",
  name = NULL
)
}
\arguments{
\item{x}{A `cdm_table` object.}

\item{indexDate}{Name of a 'date' column that indicates the index date.}

\item{window}{Window of interest.}

\item{nameStyle}{Name of the new column.}

\item{name}{Name of the new table.}
}
\value{
The table `x` with a new column column with the number of
ingredients used in the window of interest.
}
\description{
Add the maximum number of ingredients an individual is exposed simultaneouly
in a certain window.
}
\examples{
\donttest{
library(OmopIndexes)
library(omock)
library(dplyr)

cdm <- mockCdmFromDataset(datasetName = "GiBleed", source = "duckdb")

cdm$condition_occurrence |>
  slice_sample(n = 10) |>
  select("person_id", "condition_start_date") |>
  addPolypharmacyCount(indexDate = "condition_start_date")
}

}
